POSTGRES_USER=geopop
POSTGRES_PASSWORD=geopop
POSTGRES_DB=geopop

# Host port mappings (container internal ports are fixed)
DB_PORT=5432
API_PORT=8080

# Connection pool size
POOL_SIZE=32

# Used by ingestion scripts and local API development
DATABASE_URL=postgres://geopop:geopop@localhost:5432/geopop
